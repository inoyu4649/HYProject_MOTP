# 「과거의 추억」 패치 노트

> **게임**: 「과거의 추억」 · 영제 \<Memories of the Past\> · 일제 「過去の追憶」
> **브랜드**: HY Project presents · 작품 번호 #A-01
> 최신 판이 위에 온다.

---

## 표기 규약 — 이 숫자가 무엇인가

- **아직 0.x다.** 웹 데모가 서고 설치본이 나가도 이 게임은 **Early Access**다(사용자 지시
  2026-09-15). `1.0.0`은 「배포가 시작되는 날」이 아니라 **「더 얹을 것이 없는 날」**의 숫자라,
  배포 시점에 자동으로 올리지 않는다.
- **버전은 의미 있는 숫자다.** 모드가 `mod.json`에 적는 **지원 게임 버전 범위**
  (`data/mods.ts`의 `gameVersionSatisfied`)가 이 값과 견줘진다 — 9-C-2 시점의 `'1.0'`은 그냥
  화면에 찍히던 표기였고, 9-D-3부터는 판정에 쓰인다.
- **여섯 자리가 함께 움직인다**: `client/src/platform/buildIdentity.ts`의 `GAME_VERSION`(화면에
  찍히는 유일한 곳) · `package.json` 셋(루트·client·server) · `src-tauri/Cargo.toml` ·
  `src-tauri/tauri.conf.json`(설치본 파일 이름이 여기서 나온다). 어긋나도 `tsc`·`cargo`·`vite`가
  아무 말을 안 해서 `npm run check:version`이 대신 본다.
- **폐기(v0.5.0, 사용자 지시 2026-09-18) — 「내려받기 주소에는 버전이 안 적힌다」는 더 이상
  사실이 아니다.** 자동 업데이트가 "지금 최신이 몇 판인가"를 알아야 해서, 태그도 설치본
  파일 이름도 이제 **버전을 그대로** 적는다(`v0.5.0`, `MOTP_0.5.0_x64-setup.exe`) — 올릴 때마다
  새 릴리스를 만든다(`platform/distribution.ts`). 웹 데모도 이제 GitHub API를 부른다(그전에는
  「웹판이 바깥에 거는 요청은 0」이 정본이었다).

**⚠ 호환성 주의(v0.5.0)**: **모드 로드 순서의 방향이 뒤집혔다** — 0.4.x까지는 「아래에 있는
모드가 위를 덮었」지만, 이제 **「위에 있는 모드가 아래를 덮는다」**. 저장된 `motp_mod_order`는
게임이 처음 실행될 때 **한 번만 자동으로 뒤집혀** 옮겨진다 — 화면에 보이는 순서는 달라
보이지만(위아래가 뒤집혀 보인다) 어느 모드가 이기는지는 그대로다. 다만 그 순서가 뜻하는
바(어느 줄이 이기는가)가 바뀌었으므로, 여러 모드를 함께 쓰는 사람은 모드 관리 창을 한 번
열어 확인해 두는 것을 권한다.

**버전 이력**: `'1.0'`(9-C-2 — 표기뿐) → **`0.1.0`**(소급) → **`0.2.0`**(9-D-3) →
**`0.3.0`**(9-E 착수) → **`0.4.0`** → **`0.4.1`** → **`0.5.0`** → **`0.5.1`** → **`0.5.2`**.

---

## 0.5.2 — 2026-09-20

**자동 업데이트 안내 문구 정리판이다.** 두 가지가 사람을 헷갈리게 했다.

- **다운로드 중인데 "확인하는 중"이라고 떴다**: `ui/UpdateNotification.tsx`가 다운로드
  진행률 표시에 `update.checking`(확인 문구)을 그대로 재사용하고 있었다 — 새 `update.downloading`
  키로 분리했다.
- **인스톨러의 "Uninstall before installing"을 고르면 세이브가 날아가는 줄 알고 다들
  「Don't uninstall」을 골랐다**: 실제로는 세이브가 exe·res 바깥(AppData)에 있어 안전하고,
  오히려 이쪽을 골라야 실행 파일·res가 새 판으로 제대로 갈아치워진다. 다운로드가 끝나고
  인스톨러를 띄우기 직전에 `ui/UpdateInstallGuideModal.tsx`를 한 번 세워, 이 선택지를
  고르도록 명시적으로 안내한 뒤에야 설치를 시작하게 했다.

---

## 0.5.1 — 2026-09-20

**후리가나 버그 픽스판이다.** ja 로컬라이제이션이 인명·용어에 다는 `{{furigana:한자|가타카나}}`
토큰(`ui/furiganaText.tsx`)을 몇몇 화면이 `renderFuriganaText`를 안 태우고 `t()` 결과를 그대로
찍고 있었다 — 그 화면들은 루비 대신 토큰 원문이 그대로 노출됐다.

- **도전 과제**: `ui/AchievementsModal.tsx`의 격자 칸(제목·설명)과
  `ui/AchievementNotification.tsx`의 알림 토스트(제목·설명) — 예: 「明記」 계열 도전과제 제목이
  `{{furigana:明記|めいき}}された夜`로 그대로 보이던 것.
- **엔딩 서명란**: `ui/FinalEndingScreen.tsx`의 배드 엔딩 세이브 목록에서 지워지지 않은 이름 칸
  (`common:save.signature`).

나머지 ja 후리가나 사용처(대사·문서 제목·난이도명·크레딧·목표 문구 등)는 전부 점검했고 이미
`renderFuriganaText`/`parseFuriganaSegments`로 정상 처리되고 있었다.

---

## 0.5.0 — 2026-09-18

**배포 이후의 삶을 여는 판이다.** C0~C9가 닫힌 뒤(0.4.0) 처음 나가는 기능 추가판 — 의존성을
전면 최신화하고, 모드 시스템을 마크 수준으로 넓히고, 세이브가 사람을 덜 속이게 고치고, 게임이
스스로 낡았다는 사실을 알게 했다.

### 의존성 전면 최신화

모든 npm·Cargo 의존성을 **정확한 버전으로 못 박았다**(캐럿 전부 제거) — 같은 커밋이 언제
빌드해도 같은 바이너리가 나오게 하기 위해서다. React 19.3.0 · PixiJS 8.21.0 · Vite 8.3.0
(rolldown 기반) · TypeScript 6.0.3(7.0.2는 아직 보류) · i18next 26.4.2 + react-i18next 17.0.14.
Rust는 tauri 2.11.5 · zip 8.6.0 · windows 0.62.2 · winreg 0.56.0. Vite 8이 esbuild를 더 이상
hoist해 주지 않아 `scripts/build-docs.mjs`·`slim-web.mjs`가 쓰던 esbuild를 devDependency로
명시했다.

### 모드 시스템 — 마인크래프트 Forge/Mixin 수준으로

- **로드 순서 반전**: 위 「호환성 주의」 참조.
- **이벤트 버스**(`motp.events`, `capabilities:["events"]`) — 씬 이동·플래그·소지품·문서·
  목표·챕터·세이브·엔딩·도전과제·매 프레임(`tick`)까지 18종 이벤트. 핸들러마다 격리되어
  3연속 실패하면 그 핸들러만 조용히 꺼진다.
- **Mixin**(`motp.mixin`, `capabilities:["mixin"]`) — 엔딩 판정·목표·도전과제 판정·BGM 선곡·
  난이도 조율축 넷·건물 판별·소지품 조회, 11곳의 고정 대상에 `head`/`return`/`replace`로
  끼어든다.
- **상태 접근**(`motp.game`, `capabilities:["state"]`) — 진행 상태를 읽고 `dispatch`한다.
- **내부 모듈 원본**(`motp.internal`, `capabilities:["internal"]`) — 20개 내부 모듈을 그대로
  가져간다. **아무 약속도 없다** — 게임이 올라갈 때마다 깨질 수 있는 것이 정상이다.
- **모드 도전과제**(`data/achievements.json` 또는 `motp.achievements`) — 모드가 도전과제를
  더하고·고치고·뺄 수 있다. **모드가 더한 것은 Vanilla 원장이 아니라 「모드팩」(지금 켜진
  모드 조합) 전용 원장에 선다** — 모드를 지워도 바닐라 100.00%는 그대로다.
- **모드 플래그**(`data/flags.json` 또는 `motp.flags.declare`) — 모드가 자기 진행 플래그를
  더할 수 있다.
- 이 모든 것이 `capabilities` 매니페스트 선언으로 나뉜다 — 선언 안 한 창구를 부르면 조용히
  아무 일도 안 일어나는 대신 **던진다.**

### 빠진 모드 경고

모드가 켜진 채로 만들어진 세이브를, 그 모드가 빠진 채로 불러오면 **이제 알려 준다** —
`이름 (modid)` 목록과 함께, 「이해했습니다」를 눌러야 열린다. 완결 세이브를 펼쳐 볼 때도
빠진 모드 이름이 함께 적힌다.

### 난이도별 오토세이브 주기

明記·記錄(SUPER EASY·EASY)은 퍼즐·문서·아이템·방·건물·목표·챕터가 바뀔 때마다, 記憶(NORMAL)은
방·아이템·건물·목표·챕터, 記錄(HARD)은 건물·목표·챕터, 忘却(SUPER HARD)은 챕터가 바뀔 때만,
花様(EXTREME)은 **엔딩 조건에 영향이 갔을 때만** 자동 기록을 남긴다. 엔딩 조건에 영향이 가는
순간은 난이도와 무관하게 언제나 기록한다(「불러오기 신공」 방지).

### 자동/수동 업데이트

GitHub Releases API로 최신 판을 확인해(하루 한 번, 설정에서는 언제든 수동으로) 좌하단에
알려 준다. 누르면 받아서 설치본을 띄우고 게임을 닫는다. 학교·회사 네트워크 등으로 받지
못하면 수동 링크와 안내가 담긴 창이 대신 뜬다.

### 크레딧

배경음악 15곡의 Pixabay 라이선스 전문을 크레딧에 더했다(글꼴·오픈소스와 같은 자리).
YouTube Content ID 클레임이 걸렸을 때 이의 제기(Dispute)하는 방법도 함께 실었다.

---

## 0.4.1 — 2026-09-18

**스피드 플레이(단서를 빠르게 여러 개 주워 담는 진행)에서 1~6장·Final 전반에 걸쳐 재현되던
「지연 연출이 영영 안 뜬다」 계열 버그를 고쳤다.** 사용자 보고: 세이브를 불러왔을 때 옥상 문의
세 자물쇠 프롭(518274·055·9146)이 아예 없었고, 같은 증상이 청람관(4장)·호명 시작(6장) 등
여러 자리에서 반복됐다.

### 고쳐진 것

- **원인**: `GameScreen.tsx`의 「씬에 들어서고 몇 초 뒤 독백이 뜨며 진행 플래그를 세우는」
  연출들이 `useEffect` 의존성 배열에 `state.flags`(전체 플래그 객체)를 넣고 있었다. 이 객체는
  `setFlags`가 불릴 때마다 — **그 장면과 무관한 문서·아이템 습득을 포함해서** — 통째로 새로
  만들어지므로, 넣으면 그때마다 예정된 `setTimeout`이 취소되고 처음부터 다시 재는 것으로
  리셋됐다. 여러 단서를 빠르게 주워 담는 플레이에서는 그 리셋 빈도가 연출의 지연 시간(1.2~3초)
  보다 잦아지고, 그러면 독백이 영영 안 뜨고 그 독백의 `onDone`이 세우는 플래그도 영영 안 선다 —
  예컨대 정체 반전 독백이 못 떠 `finalRevealSeen`이 안 서면, `finalRooftopDoor.ts`의
  `requiresFlags`에 걸려 옥상 문 다이얼 프롭 자체가 사라진다.
- **수정**: 영향받은 17곳(Ch3 인쇄기·Ch4 사라진 졸업생·열두 개의 문·376호 문패·Ch5 재연 개방·
  옥상 문 앞 재연 중단·Ch6 마지막 열쇠·출석·목격·장 마감·Final 열두 번째 방·정체 반전·옥상
  도착·마지막 대치·엔딩 진입)에서 `state.flags`를 의존성 배열에서 뺐다. 그 장면에 실제로
  필요한 재확인 신호(씬 전환·퍼즐 창이 닫히는 것·대사가 끝나는 것·근접 판정)는 이미 각
  배열에 있었으므로, 넣지 않아도 정상적인 진행에서는 그대로 뜬다.
- **막힌 세이브 구제**: 이미 이 버그로 막힌 세이브(예: `finalRevealSeen`이 안 선 채 옥상
  층계참에 도착한 경우)는 이 패치 이후에도 자동으로는 안 풀린다 — 그 장면이 걸린 방으로
  되돌아가면(376호 재입장 등) 이번에는 정상적으로 독백이 뜨고 플래그가 선다.

---

## 0.4.0 — 2026-09-17

**C0~C9 전 챕터의 개발·테스트가 닫힌 판이다.** 9-D(다운로드판·모드)와 9-E(웹판·데모)에 하나로
묶여 남아 있던 **도보 검증**이 이 회차로 끝났고, 그로써 C9가, 그리고 계획서 §5 전체가 종결됐다.
개발 기간 표기도 `2026.07.19 — 2026.09.17`로 확정했다(`data/credits.ts`의 `DEV_PERIOD`).

### 더해진 것

- **112호 이스터에그 「112 경찰」** — 111호(게임 개발사 111%)·119호(소방서)와 같은 줄의 한국
  번호 밈이다. 청람관 호실 이스터에그가 스물여덟 → **스물아홉**이 됐다
  (`data/dormEasterEggs.ts`). 『한국인 인증』 도전과제는 `K` 태그에서 파생하므로 코드는 이 한 줄만
  더하면 따라온다.
- **[I-01] 「책상 밑 낙서」가 실제로 놓였다.** 본문은 `data/documents.ts`에 있었는데 어느
  스테이지도 `grantsDocuments`로 걸지 않아 **디버그 메뉴로만 얻을 수 있었다** — 즉 정직하게
  플레이해서는 도감 100%가 이론상 불가능했다. 도감표(`data/docCatalogue.ts`)가 적어 둔 자리 그대로
  **3-2 교실 책상 밑**에 놓았다: `ch1Classroom.ts`의 `textbook-desk` 프롭에 관찰형 스테이지
  `deskGraffiti`를 얹어, 교과서 필기를 다 읽은 뒤(`requiresFlags: ['locker16Unlocked']`) 같은
  책상을 다시 조사하면 나온다. 이 프롭은 Ch5의 같은 교실(`ch2Classroom32.ts`)에도 좌표만 갈아
  끼워 재사용되므로 **두 챕터 모두에서** 얻을 수 있다.

### 바뀐 것

- **5·6·7층(남자 기숙사층)에서 붙잡히면 더 무겁게 걸린다**(`data/dormPatrol.ts`). 정윤서 자신이
  여학생이라 그 층에 서 있는 것 자체가 1~3층보다 무거운 무단 침입이라는 설정을 그대로 규칙으로
  옮겼다 — 이스터에그 말고는 볼일이 없는 층이라 고난도로 잡아도 무방하다(사용자 지시).
  - 들킴 게이지가 그 난이도 기본 속도의 **1.5배**로 찬다(`floorGaugeFillMultiplier`).
  - 붙잡혔을 때 사감의 훈계 대사가 다르다(`ch4:patrol.scoldBoysFloor`, 세 번째부터는
    `scoldSevereBoysFloor`).
  - 압수당하는 단서가 두 장이 아니라 **세 장**이다(`CONFISCATE_COUNT_BOYS_FLOOR`).
  - 순찰 걸음 속도와 사감실 위치(1·3층 규칙)는 **그대로다** — 바뀌는 것은 「들키는 속도」와
    「들켰을 때의 대가」뿐이다.
- **「기록해 둔다」 버튼에 소리가 붙었다.** 이 버튼만 눌러도 무음이었던 실결함이다: `PuzzleModal`의
  `Panel`이 바깥 클릭으로 창이 닫히는 걸 막으려 `onClick`에서 `e.stopPropagation()`을 걸어,
  `App.tsx`의 **위임 클릭 효과음이 이 창 안까지 애초에 못 들어왔다.** 위임과 무관하게 버튼에서
  직접 재생하도록 고쳤고, 소리도 일반 딸깍(`ui-click.ogg`)이 아니라 **적어 두는 소리**
  (`sfx/write.ogg`)로 구분했다.

### 고쳐진 것

- **명찰이 번져 글자를 아예 못 읽는 동안은 「기록해 둔다」를 못 누른다**(`useNameTagBlur`의
  `unreadable`). 못 본 것을 적어 두는 행위는 관찰 퍼즐의 전제와 어긋난다 — 버튼을 비활성화하고
  도감과 같은 「글자가 번져 있다」 문구를 대신 띄운다.

### 남는 것

- `res/audio/sfx/write.ogg`는 **코드만 걸려 있고 음원 파일은 아직 없다**(`res/audio/sfx/README.md`
  참조). 전 구간 무음 폴백이 있어 없어도 정상 동작한다.

---

## 0.3.0 — 2026-09-15 · C9 Part 9-E

**웹판이 선 판이다.** 원래 계획서 §5의 한 줄(웹 빌드 + `deploy/` 확인)에 9-C·9-D가 미뤄 둔 둘과
실측이 불러낸 하나가 붙어 다섯 갈래가 됐다.

### 웹판 = 프롤로그 데모

- **웹으로 배포되는 판은 프롤로그까지다**(`data/demo.ts`). 웹 기본 채널이 `itch` → **`demo`**로
  바뀌었다.
- **막는 자리는 `ui/EndingScreen.tsx` 한 곳뿐이다.** 프롤로그는 출구가 하나라 「본편에 반쯤 걸친
  데모」가 성립하지 않는다 — 장마다 검사를 뿌리면 언젠가 한 갈래가 빠지고, **그 갈래로 들어간
  사람은 본편 한복판에서 막혀 데모가 아니라 고장으로 읽힌다.**
- `completePrologue()`를 안 부르므로 **「본편 세이브를 만들어 두고 못 들어가는 상태」가 안 생긴다.**
  컷신까지 똑같이 보여 준 뒤, **벚꽃잎이 내려앉은 같은 화면 위에** 「여기까지」와 내려받기 버튼이
  이어 붙는다(`ui/DemoEnd.tsx`).
- **개발 서버만은 예외다** — 웹 기본값을 `demo`로 바꾼 순간 `npm run dev:client`까지 데모가 돼서
  본편을 만드는 사람이 본편에 못 걸어 들어갔다. 기본값을 `IS_DEV ? 'itch' : 'demo'`로 두고, 데모
  자체를 걸어서 확인할 때는 `npm run dev:demo`를 쓴다.

### 배포 정본 — 주소에 버전을 안 적는다

- `platform/distribution.ts`가 **「PC판 내려받기」가 가리키는 곳을 아는 유일한 곳**이다.
- **태그도 파일 이름도 `latest` 고정**(사용자 지시). 배포된 웹 데모는 게임보다 오래 살아 있어서,
  주소에 `0.3.0`이 박히면 **게임을 한 번 올릴 때마다 링크가 죽는다** → 움직이는 쪽을 주소가 아니라
  릴리스로 옮겼다.
- 그 덕에 **버전을 물어보는 네트워크 요청이 아예 없다**(GitHub API를 한 번 만들었다 지웠다 —
  API 한도라는 실패 모드가 통째로 사라졌다). **웹판이 바깥에 거는 요청은 0이다.**
- 릴리스 목록 페이지(`releasesPageUrl()`)를 함께 둔다 — 자산 이름이 바뀌었거나 그 릴리스가 잠깐
  비어 있어도 사람이 제 손으로 찾아갈 길은 남아야 한다.

### 경량화 — 실측 111MB → 20.7MB

두 겹이고 **엄격함이 반대다**: 프리로드 범위는 「미리 받기」만 줄여 틀려도 「한 박자 늦게 뜬다」에
머무르므로 **빼는 쪽**으로 적고, 산출물 덜어내기는 「영영 없다」라서 **남길 목록에 적힌 것만** 살린다.

- 정본은 `data/demoAssets.ts` 하나 — import 없는 잎 파일이라 런타임과 `scripts/slim-web.mjs`가
  **같은 한 벌**을 읽는다.
- **폰트 27.7MB → 3.7MB**(HWYWonSerif 13.9MB → 1.4MB). 소스에 적힌 **2,690자만** 남긴다.
  전제는 「데모에 타자 퍼즐이 없다」이고, 그 퍼즐이 데모에 들어오면 `U+AC00-D7A3`을 되돌려야 한다.
  훑기로 못 잡는 「오전/오후」(`toLocaleString`)는 따로 챙겼다 — 안 챙기면 **기록 목록의 시각에서만**
  두부가 뜬다.
- **죽은 무게 17.5MB 발견** — `film_rooftop_01~06`·`photo_1938_planting`이 매니페스트에도 소스에도
  안 걸린 채 **웹·데스크톱 양쪽에 실려 나가고 있었다.**
- 폰트 줄이기는 Python + `fonttools`가 있을 때만 돈다. 없으면 건너뛰고 경고만 남기며, 배포 자체는
  정상이다(그냥 27MB 더 무겁다).

### 배포 파이프라인

- **`server/src/index.js`의 캐시 헤더가 전부 `no-store`였다** — 새로고침 한 번마다 게임 전체를 다시
  받고 있었다. `/assets/*`는 1년 + `immutable`(해시 이름이라 안전) · `/res/*`는 1시간 +
  `must-revalidate` · `index.html`과 `/api/*`는 그대로 `no-store`.
- **함정**: `express.static`도 `sendFile`도 안쪽이 `send`라 자기 헤더로 덮어쓴다 → 같은 함수를 세
  자리에서 부른다.
- `npm run build:web`이 ①버전 정합 검사 ②`vite build` ③경량화를 순서대로 한다. `deploy/` 스크립트
  넷이 이걸 부른다.

### 검사기

- **`scripts/check-version.mjs` 신설** — 여섯 자리 정합. 9-D의 「Tauri 명령을 세 곳에」와 **정확히
  같은 모양의 함정**(어긋나도 모든 그린 체크가 통과한다)이라 같은 방식으로 막았다.
  `build:web`·`build:desktop`이 먼저 돌린다.

### 9-D에서 이어받아 마저 닫은 것

- 모드 표식이 **완결 세이브에만 있고 일반 기록에는 없었다**(2026-09-10 지시의 절반). `SaveMeta.mods`
  + `isModdedStamps`로 `LoadModal`·`SaveModal`의 auto·수동 1/2/3에도 **같은 남색 · 같은 MODDED 배지**를
  붙였다. 표시가 갈리면 목록에서 「어느 쪽이 모드였는지」를 알 수 없다.

---

## 0.2.0 — 2026-09-15 · C9 Part 9-D

**다운로드판이 선 판이다.** §3-6이 모드 계통 전부를 여기로 미뤄 둬서(2026-07-22 확정) 빌드 분기와
모드가 한 Part에 붙었다.

### 다운로드판(Tauri)

- Rust 셸 + WebView2. **전체화면 전용**, `decorations: false`, `theme: Dark`.
- 해상도 자동 확대(`platform/renderScale.ts`), 외부 `res/`
  (`desktopBridge.initDesktopResRoot()`가 `executableDir()` + `convertFileSrc()`로 채운다).
- **그래픽 설정** 신설 — 렌더러 API + GPU 선택. 값이 **실제로 바뀔 때만** 재시작 모달을 띄운다.
- **배포물은 `MOTP.exe` + `res/` + `docs/` 딱 세 개다**(실행 중에 `save/`·`logs/`·`mods/`가 그
  옆에 생긴다).

#### C1부터 두 달 끌던 하얀 깜빡임이 여기서 끝났다

원인은 캔버스가 아니라 **캔버스를 「다시 만드는 것」**이었다 — WebView2가 새 `<canvas>`를
DirectComposition 비주얼로 승격시키는 그 한 틱. 브라우저 플래그로 막으면 WebGPU가 회색 노이즈가 되는
부작용이 드러나, **캔버스를 다시 만들지 않는 쪽**으로 갔다(`game/pixiApp.ts`): `app.init()`은
프로세스 수명에 딱 한 번, 씬 전환은 `renderer.resize()` + 배경색만, 씬 사이에 destroy 안 함.

> **다시 시도하지 말 것**: `renderer.prepare.upload()`는 pixi.js 8.19.0 + 전부 절차적 Graphics라는
> 이 프로젝트 조합에서 `DefaultBatcher.break`가 죽는다.

### 저장소의 갈림 — localStorage → 파일

- 범용 `plugin-fs`가 아니라 **전용 Rust 명령 넷**(`src-tauri/src/save.rs`): 키 하나당 파일 하나,
  쓰기는 **임시파일 + rename으로 원자적**, 키 문자 제한으로 경로 탈출 방지.
- 프런트 `save/storage.ts`가 **동기 API 외피**(메모리 캐시 + 400ms 디바운스 flush)로 감싸, 기존 세
  소비처가 웹과 **똑같은 동기 호출**로 쓴다. 종료 전 `flushPending()`을 기다린다.

### 모드 계통

정본은 `data/mods.ts`(제약 열) + `mods/` 넷 + Rust `src-tauri/src/mods.rs`.

- **핵심 설계 결정**: 「**로더는 모으기만 하고, 병합은 그 데이터를 든 모듈이 스스로 자기 평가 시점에
  한다**」 — 임포트 순서를 규약으로 삼으면 언젠가 조용히 깨진다. 예외는 `data/difficulty.ts`
  하나(`main.tsx`가 `profile.ts`를 정적 임포트해 로더보다 먼저 평가된다 → 거기만 **읽을 때** 얹는다).
- **오버라이드 갈래 아홉**: 그림 · 소리 · 맵 · 글꼴 · 목표 · 대사 · 난이도 · Advanced · 언어 추가.
- **지원 게임 버전 범위가 안 맞아도 거부가 아니라 경고다** — 모드는 게임보다 오래 산다.
- Advanced는 `eval`이 아니라 `new Function('motp', src)`.
- **도전과제 정책**: ⚠(덮었거나 코드를 실은) 모드가 켜져 있으면 안 서고, **더하기만 하는 모드는
  선다.**
- **오류(Crash) 화면** — React 없이 순수 DOM, `<style>`도 직접 꽂고, **글도 i18n이 아니라 파일 안에
  적는다**(이 프로젝트에서 화면 문자열을 로케일 밖에 적는 유일한 자리).
- **Strict Mode**(기본 꺼짐) — `loadMods()`가 **던지는 대신 거부를 돌려주고**, 멈추는 주체를 사람의
  설정으로 둔다. Safe Mode가 켜져 있으면 판정 자체를 안 한다.

### 모드 관리 화면

`ui/ModsModal.tsx` — 순서 · On/Off · ⚠ · 실패 모드 · 버전 불일치 경고 · Safe/Strict 토글 ·
Advanced 동의 · 「모드 다시 읽기」.

- **`mods/`가 비어 있으면 설정의 그 한 줄조차 안 뜬다.**
- 목록의 On/Off는 `rec.enabled`(이번 부팅의 결과)가 아니라 **`readSavedOrder()`(사람의 뜻)**를
  읽는다 — 안 그러면 Safe Mode인 채로 줄 하나만 옮겨도 저장 목록이 「전부 꺼짐」으로 덮인다.
- 자동 Safe Mode는 **영속화한다** — 안 적으면 네 번에 한 번꼴로만 게임에 들어오는 고리가 된다.
- **바꿔도 런타임에 안 되돌린다.** 되돌릴 수 있는 척하지 않고 「다시 읽거나 다음 실행부터」라고 그
  자리에 적는다.
- 모드가 적용된 채 기록된 **완결 세이브**는 배경이 남색이 되고 우상단에 `MODDED` 배지가 붙는다
  (일반 기록까지 붙은 것은 0.3.0).

### 법적 문서 조립

- `scripts/build-docs.mjs`(`npm run build:docs`)가 **정본 셋만 읽어**(`credits`·`licenses`·
  `fontLicenses`) `dist/docs/`를 조립하고, **개발 문서 문자열이 섞이면 즉시 throw**하는 화이트리스트
  원칙을 코드로 지킨다.
- Rust 쪽 라이선스 3건의 드리프트(`tauri-build` 누락 · `windows-rs` → `windows` · `winreg` 버전)를
  실측으로 찾아 고쳤다.

### 🔴 사고와 교훈 — Tauri v2는 명령을 세 곳에 적어야 한다

prod 빌드에서 `mods/`를 아예 인식 못 했다. 적어야 할 세 곳은 ① `lib.rs`의 `generate_handler!`
② `build.rs`의 `AppManifest::commands` ③ `capabilities/default.json`의 `permissions`. ①에만 적어
`invoke('list_mods')`가 **런타임에 조용히 거부**됐고 — **`cargo check`도 `tsc`도 전부 통과한다.**

고친 길 다섯: `build.rs`·capabilities 보강 · **`scripts/check-tauri-commands.mjs` 신설**
(`dev:desktop`·`build:desktop`이 먼저 돌린다) · `modRegistry.loaderError`로 「0개」와 「못 읽었다」를
가름 · 로더가 **언제나** `mods_root`와 발견 개수를 `logs/`에 적음 · `allow_directory` 실패를 삼킴
(안 그러면 `mods/`가 없는 보통 설치본에서 창이 아예 안 뜬다).

> **삼키는 것은 옳지만, 삼킨 사실까지 감추면 안 된다** — 이 사고의 교훈이다.

---

## 0.1.0 — 2026-09-14 · C0 ~ C9 Part 9-C

**게임 한 벌이 처음으로 끝까지 서 있게 된 판이다.** 번호는 **소급해서 붙인 것**이다 — 이 시점의
`GAME_VERSION`은 9-C-2가 화면 좌하단에 찍으려고 둔 `'1.0'`이라는 **표기뿐**이었고, 판정에 쓰이는
숫자는 0.2.0부터다.

### 기반 — C0 (2026-07-21)

- **i18n**: i18next + react-i18next + languagedetector, **ko / en / ja 3언어**. 챕터 단위
  네임스페이스 lazy-load, `<html lang>` 연동.
- **코어 계통 아홉**: SceneManager(다중 맵 파서·전환) · SaveSystem(슬롯 3 + 오토세이브 + 마이그레이션)
  · FlagStore · Archive(문서 도감 + 번역 토글 뷰어) · Inventory · DialogueSystem · AudioManager(무음
  폴백 — **음원이 없어도 전 구간 진행된다**) · DebugMenu · TitleScreen.
- **플랫폼 분기 계층**(`client/src/platform/`) — `buildTarget` · `verify`(서버 없이도 정답 확인) ·
  `assetUrl`. **C9의 빌드 분기를 C0에서 미리 받아 둔 자리다.**
- 리브랜딩 — 오프닝 스플래시, 실세계 축제 문구 전수 제거(§7 체크리스트). **유일한 예외는 엔딩
  크레딧의 원작 표기**다.

### 프롤로그 — C1 (2026-07-22)

3-2 교실 재구축(15×11 ASCII 정본 유지), **P-01~P-04** 이식, 15분 시계와 힌트 2회, 손전등 · 0.5초
조우 · 점프스케어, 탈출 뒤 **벚꽃잎 한 장** → 「사물함 안을 확인해 봐」 이행 컷신. 3언어 도보 주파로
확인했다.

> 프롤로그 세 정답(518274 / 055·033·087 / 9146)과 기존 자산(유언장·명단·앨범·수능특강)은 **수정·재해석
> 금지 정본**이다.

### 착수 전 정비 (2026-07-24)

- **폰트 3계통 개편** — `--font-scare`(연출) / `--font-hand`(손글씨) / `--font-ui`(인쇄체). 전 폰트
  **로컬 woff2 번들 8종**, CDN 미사용. 라이선스는 `docs/license/`에 전량 확인.
- **브랜드명 확정** — placeholder 「井結GAMES」 → **HY Project**.
- **명단·앨범 실사 자산 교체** — 34인 학번 사진 + 단체사진 2장.

### 본편 여섯 장 — C2 ~ C7

| 장 | 무대 | 이 장이 처음 세운 것 |
|---|---|---|
| **Ch1 「사물함」** (C2, 08-05) | 3-2 교실 · 동쪽 복도 | **아이템 계통**(소지품 탭 · 정사각 그리드 · 폐기 불가) · **개발자 모드**(정식 빌드에도 있는 상시 토글) · **뷰포트 1280px 확장** · 핸드폰 UI · P-05~P-09 |
| **Ch2 「본관 — 서류의 무덤」** (C3, 08-18) | 본관 전층 + 특별관 2·3층 | **카메라(스크롤) 도입** — 125×6 복도가 한 화면에 안 들어간다 · **NPC 계통** · **길 안내 계통**(위치 표시기 → 맵 → 다음 목표) · **수위실 허브**(세이브 · 난롯가 대화 = 본편 힌트) · P-10~P-15 · 방송실 탈출 추격 |
| **Ch3 「특별관 — 멈춘 인쇄기」** (C4, 08-22) | 특별관 전층 | **암실 빛 반전 규칙**(어둠 안전 / 빛 위험) · **승강기** · **3대 증거 집계**(세지 않고 파생한다) · P-16~P-20 · 새벽 3시 인쇄기·백지 연출 · 암실 후공정 미니게임 |
| **Ch4 「청람관 — 열두 개의 문」** (C5, 08-26) | 청람관 7개 층 | **호실 이스터에그 + 도전과제** · **사감 순찰**(들킴 게이지 · 단서 압수) · **열두 개의 문**(동창 12인 기억 방 369~380) · 「겹」 정본 · P-21~P-25 |
| **Ch5 「강당·체육관 — 팔월의 그날」** (C6, 08-29) | 강당 · 체육관 · 옥외 | **재연(과거 관찰) 계통** — 2027년 8월 시간축, 흐린 얼굴과 선명한 한 명 · **시간축**(1~6교시가 퍼즐 넷을 연다) · P-26~P-30 · 옥상 문 앞의 끊김과 **「죄의 인정」** |
| **Ch6 「지하 — 명부」** (C7, 09-08) | 본관 지하 · 구관 지하 | **BGM 계통**(`data/bgm.json` — 화면·맵별 지정 · 패턴 · 폴백) · **출석 스텔스**(호명 · 3회면 배드 직행) · **세이브 불가 구역** · 백지 서가 · P-31~P-35 |

### 마지막 장과 엔딩 — C8 (2026-09-10)

- **Final** — 옥상 · 벚꽃길 · 정윤서의 방(정체 반전 2단계 컷신) · **P-36~P-39**.
- **엔딩 다섯** — True 「벚꽃」 · Good · Normal · Bad(세이브 슬롯이 **空欄**이 되는 4번째 벽 연출) ·
  Secret. 갈래는 저장하지 않고 **엔딩 분기 변수 다섯에서 파생한다**(`data/endings.ts`).
- **엔딩 크레딧** — 메뉴 크레딧과 **같은 목록을 읽는다**(`data/credits.ts`). 원작 표기(축제 언급이
  허용되는 **유일한 자리**, 프롤로그 한정) · 생성형 AI 고지 · 개발 기간. **등장인물의 이름은 크레딧에
  올리지 않는다** — 마지막 화면에서 이름이 목록이 되면 P-38이 한 일이 통째로 값싸진다.
- **경과 시간**이 세이브에 남아 크레딧 마지막 장에 뜬다(`game/playClock.ts` — 모듈 시계, 리렌더 0회).
- **회차 인계** — 벚꽃잎 책갈피 [I-11], 낡아 가는 타이틀.

### 히든과 마감 — C9 Part 9-A ~ 9-C

- **9-A · P-40 「과거의 이상」**(09-11) — True 뒤에 열리는 2027년의 what-if. 무대는 3학년 2반 한
  곳이고, 네 걸음이 Ch1의 네 걸음을 되짚는다. 완결 세이브의 **여섯째 색**(True + 히든 = 온통 분홍)도
  여기서 섰다.
- **9-B · 문서와 도감**(09-11)
  - **정본 `data/docCatalogue.ts`** — 바이블 10장의 **106종**을 코드가 읽는 한 벌로. 그전까지
    `documents.ts`는 「쓴 것」만 알았고, **못 모은 것을 셀 방법이 없었다.**
  - **잔여 32종 집필·배치 완료** — `docKey: null` **0**. 급식실 등 새 무대 셋이 이때 섰다.
  - **도전과제 50종** — 다섯 분류(기억 · 수집 · 기록 · 자취 · 히든) · 아이콘 · 달성음.
    『100%』와 『100.00%』가 갈린다.
  - **en/ja 최종 검수** — **2,380키 × 3언어** 전수 감사. 가장 큰 수확은 번역문이 아니라 **렌더러**였다
    — ja 후리가나 토큰이 네 곳에서 **날것으로 화면에 찍히고 있었다**.
- **9-C · 콘텐츠 마감**(09-14, **도보 플레이테스트까지 통과**)
  - **난이도 여섯** — 明記 · 記錄 · 記憶 · 褪色 · 忘却 · **花樣**. **난이도가 바꾸는 것은 안내 · 손 ·
    표식 셋뿐이고 엔딩 조건은 한 글자도 안 바뀐다.** 明記·記錄에서는 도전과제가 안 선다.
    해금은 단조롭다(褪色 = 2회차부터 · 忘却 = Good·True 목격 · 花樣 = 忘却 True + 히든 + 『100%』).
    **초고난도로 갈수록 읽는·듣는·안내받는 시간은 깎되 갈래의 조건과 두 물음은 안 깎는다.**
    Ch2 추격은 **기준 속도를 절대 안 올린다**(이미 플레이어와 동률이라 ×1.15만 해도 「필연 피격」이
    된다) — 지연 · 초기 간격 · 완급의 비대칭만 움직이고 DEV 감사기가 「좁히는 총량 < 초기 간격」을
    지킨다.
  - **빌드 표기** — `platform/buildIdentity.ts` 신설. 타이틀 좌하단 한 줄(채널 넷 · `GAME_VERSION` ·
    `ENGLISH_TITLE` · `modBadge()`). **`modBadge()` 함수 하나만 9-D-3이 바꿔 끼워 줄 전체가 따라왔다.**
  - **오디오 마감** — 미연결 효과음 여섯 연결 · **`sfxVolume` 신설**(`masterVolume`만으론 효과음 단독
    음소거가 안 됐던 실결함) · `data/sfx.json` + `data/sfx.ts` 신설.
  - **밸런스·성능·모바일** — **코드 변경 없음을 확인한 Part다**: 밸런스 상수엔 이미 실측 근거가 주석에
    있고, `CullerPlugin` + `cullable` + `cullArea`가 이미 정확한 패턴이며, 난이도 선택창과 조이스틱/HUD는
    **동시에 뜨는 경로 자체가 없다.**

### 이 판까지의 규모

맵 **162장** · 퍼즐 **P-01~P-40** · 문서 **106종** · 도전과제 **50종** · 엔딩 **다섯 갈래** ·
난이도 **여섯** · 로케일 **2,380키 × 3언어**.
